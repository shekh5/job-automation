# Gap Notes

Use short notes like:

- ATS endpoint returned no current listings.
- Career page hid jobs behind filters and search was needed.
- Search found a listing, but the apply URL was stale.
- Company should be rechecked next run because the page changed.
